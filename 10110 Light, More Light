#include <stdio.h>
#include <math.h>

int main()
{
    long long n, i, j;


    while(scanf("%lld", &n)==1)
    {
        if(n==0) break;

        i = sqrt(n);
        j = i*i;

        if(j==n) printf("yes\n");
        else printf("no\n");
    }
    return 0;
}
