#include <stdio.h>
#include <stdlib.h>

int main()
{
    long long n, i;

    while(scanf("%lld", &n)==1)
    {
        if(n==0) break;

        else if(n<=100) printf("f91(%lld) = 91\n", n);
        else if(n>=101) printf("f91(%lld) = %d\n", n, n-10);
    }
    return 0;
}
